# Профилирование открытия и чтения `shot_table/all__v3.lance` на текущем `main`

Дата: 2026-09-15. Версия Lance: `main`, коммит 84810db2b (13.0.0-beta.1), совпадает с `origin/main`.
Таблица: `s3://ai-lumalabs-datasets-ap-se-2/lance_datasets/shot_table/all__v3.lance` (регион ap-southeast-2, `stable_row_id=true`, версия 314).

Инструменты: Rust-харнесс поверх крейта `lance` (`--profile bench`: opt-level 3 + debuginfo), `perf record -e cpu-clock` (user-space), `strace -f -c` и `strace -e connect`, chrome-trace спанов `tracing` из `lance-io`, разбор манифеста на уровне protobuf wire-format, свежесобранный pylance из того же коммита.

## 1. Сводка замеров

| Операция | Время | Сеть | Память |
|---|---|---|---|
| `DatasetBuilder::load()` (холодное) | 2.5–3.5 с | 60 GET, 945 МБ, 57 TCP-соединений | RSS 1.0 ГБ, пик 1.9 ГБ |
| Повторное открытие с общим `Session` | 0.07 с | 1 LIST | — |
| Декодирование манифеста без сети | 0.82 с CPU | — | пик 2.0 ГБ |
| Построение `RowIdIndex` (первый take) | 0.7 с | 0 (row_id inline) | +0.9 ГБ |
| take 1000 row_id / 10 фрагментов / 5 колонок, тёплый | 0.6–0.7 с | ~1900 GET, 82–95 МБ | — |
| take 100 row_id / 100 фрагментов, тёплый, кэш 1 ГиБ | 0.55–1.8 с | 497–689 GET | +0.9 ГБ при пересборке индекса |
| то же, кэш метаданных 8 ГиБ | 0.27–0.32 с | 497 GET | стабильно |
| скан 20 фрагментов / 5 колонок, batch 1024 | 1.36 с, 11.5 млн строк/с | 601 GET, 910 МБ | — |
| то же, batch 8192 | 0.67 с, 23.4 млн строк/с | 601 GET, 910 МБ | — |
| скан 4 фрагмента, все 104 колонки | 169 с | 46 816 GET, 315 ГБ | пик 10 ГБ |

Python-обёртка накладных расходов не добавляет: take 0.62 с и скан 10 млн строк/с на pylance из этого же коммита совпадают с Rust-харнессом.

## 2. Устройство таблицы: почему манифест весит 945 МБ

Разбор последнего манифеста `_versions/18446744073709551301.manifest`:

| Поле манифеста | Байты |
|---|---|
| `fragments` (20 760 шт.) | 944 903 127 |
| `schema_metadata` | 32 551 |
| `fields` (157 полей, 104 верхнего уровня) | 6 329 |
| всё остальное | < 300 |

Внутри фрагментов:

| Поле | Байты | Количество |
|---|---|---|
| `inline_row_ids` | 927 263 229 | 20 760 |
| `files` | 13 988 713 | 102 499 |
| `inline_last_updated_at_versions` | 2 847 028 | 20 760 |
| `inline_created_at_versions` | 269 488 | 20 760 |

- 102 499 data-файлов формата 2.0 (в футере `v0.3`), 3–12 файлов на фрагмент, у всех известен `file_size_bytes`, поэтому HEAD-запросов при открытии файлов нет.
- 18 180 388 986 физических строк, 0 deletion-файлов, медиана 1.05 млн строк на фрагмент, максимум 1.2 млн.
- Максимальный row_id 19.49 млрд: ~7% id уже удалены.

Типы сегментов row_id:

| Тип сегмента | Сегментов | Байт | Строк |
|---|---|---|---|
| `range` | 11 978 | 143 736 | 10.22 млрд |
| `range_with_bitmap` | 9 734 | 889 806 202 | 7.12 млрд |
| `range_with_holes` | 3 488 | 36 699 362 | 2.13 млрд |
| `sorted_array` | 12 | 470 756 | — |

**98% манифеста — битмапы по одному биту на строку**, результат компакций фрагментов, в которых были удалённые строки. В битмапах 18% нулей (1.28 млрд дыр), но дыры кластеризованы: всего 52.9 млн run-ов. Run-length кодирование дало бы ~212 МБ вместо 890 МБ.

Максимальная глубина перекрытия диапазонов row_id между фрагментами — 34. Порог в `rust/lance-table/src/rowids/index.rs` (`MAX_PROBE_DEPTH = 64`) не превышен, поэтому `RowIdIndex::new` выбирает probe-режим и не строит слитую карту по всем 18 млрд строк. Это важно: если после дальнейших компакций глубина перевалит за 64, построение индекса станет O(всех строк) и займёт десятки секунд.

Служебные данные: 314 сохранённых версий с манифестами по ~945 МБ, 319 transaction-файлов на 48 ГБ (до 2.7 ГБ каждый), 595 директорий в `_indices/` (например, `auxiliary.idx` на 8.6 ГБ), при этом `list_indices()` в текущей версии возвращает 0.

## 3. Открытие датасета

Путь: `DatasetBuilder::load` → `resolve_latest_location` (один LIST `_versions/`) → `Dataset::load_manifest` → `read_last_block` (64 КБ) → `read_struct`/`read_message` → 59 параллельных GET по 16 МБ (`METADATA_READ_CHUNK_SIZE`, `buffered(io_parallelism = 64)`) → `pb::Manifest::decode` → `Manifest::try_from` → `check_manifest_storage_contract` → `checkout_manifest`.

Сеть: 60 GET через 57 соединений, суммарно ~600 МБ/с, ~1.5 с. Далее ~0.8 с последовательного CPU.

CPU-профиль открытия (`perf`, 2.2 с сэмплов на 2.5 с wall):

| Символ | Доля |
|---|---|
| `__memmove_avx_unaligned_erms` | 29% (64% в чистом декодировании без сети) |
| `_aesni_ctr32_ghash_6x` (TLS) | 9% |
| malloc / free / consolidate | ~9% |
| tokio/hyper/futures каналы | ~8% |
| prost `int32::merge` (`fields`, `column_indices`) | ~6% |
| SipHash интернера полей `DataFileFieldInterner` | 4% |

Байты row_id копируются трижды:

1. `read_message` в `rust/lance-io/src/utils.rs` собирает чанки в один `BytesMut` через `extend_from_slice` (945 МБ).
2. prost декодирует `inline_row_ids` в `Vec<u8>` (сгенерированный `InlineRowIds(Vec<u8>)`, `bytes` не настроен в `build.rs`), ещё 927 МБ.
3. При построении `RowIdIndex` `RowIdSequence::try_from` копирует битмап в `Bitmap { data: Vec<u8> }`, ещё 890 МБ.

После открытия RSS ~1 ГБ: inline-байты живут в `Fragment.row_id_meta` (`InlineRowIds` внутри `Arc`, клоны фрагментов дёшевы). Пик 1.9 ГБ = сырой буфер + декодированные `Vec<u8>` до освобождения буфера.

Остальные шаги открытия (`compute_fragment_offsets`, проверка `row_id_meta.is_some()` для всех фрагментов, `check_manifest_storage_contract` по 102 499 файлам с дедупликацией через интернер, `fragment_bitmap` из 20 760 id) в профиле не видны, на этой таблице они укладываются в миллисекунды.

## 4. Take по stable row_id

Путь: `TakeBuilder::execute` → `get_row_id_index` → `load_row_id_index` (декодирует все 20 760 последовательностей через `buffer_unordered(64)`, CPU-only, потому что row_id inline) → `RowIdIndex::new` → `get_many` → группировка адресов по фрагментам → `FileFragment::open` → `read_range_as_batch`/`take_as_batch`.

### 4.1 Индекс row_id и кэш метаданных

- Построение индекса: 0.7 с холодное, 0.41 с повторное, 5 мкс при попадании в кэш. Запись весит 955 888 528 байт.
- Манифест (~950 МБ) и индекс (~955 МБ) вместе не помещаются в кэш метаданных по умолчанию (`DEFAULT_METADATA_CACHE_SIZE` = 1 ГиБ) и вытесняют друг друга: `CacheStats { hits: 1, misses: 3, num_entries: 1 }` после трёх запросов индекса.
- При касании 100 фрагментов метаданные их файлов (200 записей) вытесняют индекс, он пересобирается, RSS прыгает на +0.9 ГБ:

| Кэш метаданных | take #0 | #1 | #2 | #3 |
|---|---|---|---|---|
| 1 ГиБ (по умолчанию) | 2.56 с | 1.79 с | 0.93 с | 0.55 с |
| 8 ГиБ | 2.59 с | 0.28 с | 0.32 с | 0.27 с |

- `load_row_id_index` читает последовательности через `read_row_id_sequence`, минуя per-fragment кэш `load_row_id_sequence`. Скан с `with_row_id` и take поэтому держат в памяти две независимые декодированные копии (по ~890 МБ) поверх inline-байтов манифеста.
- Без `e_tag` у манифеста индекс не кэшируется вовсе (`get_row_id_index`); для S3 `e_tag` приходит из листинга, здесь это не срабатывало, но стоит помнить для других хранилищ.

### 4.2 Сетевые запросы тёплого take

1000 строк из 10 фрагментов, 5 колонок: 0.6–0.7 с, ~1900 GET, 82–95 МБ трафика ради 61 КБ результата. Распределение размеров GET из chrome-trace `get_range`:

| Размер GET | Количество |
|---|---|
| ≤ 64 байт | 1048 |
| ≤ 4 КБ | 23 |
| ≤ 64 КБ | 439 |
| ≤ 1 МБ | 370 |
| > 1 МБ | 1 |

Латентность GET: p50 148 мс, p90 207 мс при ~700 одновременных запросах (при ~150 одновременных p50 падает до 27 мс). Wall-time take ≈ 4–5 последовательных сетевых кругов.

По колонкам (1000 строк, тёплый):

| Колонка | Тип | GET | Трафик | Время |
|---|---|---|---|---|
| `key` | utf8 | 1033 (687 ≤ 64 Б) | 28 МБ | 0.78 с |
| `shot_uuid_v2` | utf8 | 399 (268 ≤ 64 Б) | 16 МБ | 0.63 с |
| `start_time` | float32 | 153 | 21 МБ | 0.50 с |
| `duration` | float32 | 153 | 22 МБ | 0.56 с |

- Строковые колонки в файлах 2.0 требуют два зависимых чтения на строку: offsets (16–33 байта), затем байты. Это источник микро-GET и лишнего круга латентности.
- Float-колонки читают ~25 МБ на 1000 значений: `FileScheduler::submit_request` склеивает диапазоны, отстоящие меньше `block_size` = 64 КБ (`is_close_together`). При 100 случайных строках внутри страницы на 1.2 млн значений средний зазор ~48 КБ, почти всё склеивается в чтения по 200–600 КБ. Для S3 это разумный обмен IOPS на трафик.
- Открытие фрагмента стоит 1 GET на data-файл (64 КБ хвоста; метаданные файла на 124 колонки занимают 27 КБ и помещаются в него), файлы одного фрагмента открываются последовательно (`open_readers_impl`, `rust/lance/src/dataset/fragment.rs:1329`).
- CPU на take невелик: 6 take + открытие = 6.3 с user + 5.1 с sys при 9.2 с wall; sys уходит в TLS и приём данных.

## 5. Скан фрагментов батчами

| Сценарий | GET | Трафик | Время | Строк/с |
|---|---|---|---|---|
| 2 фрагмента, 5 колонок, batch 1024 | 28 | 65 МБ | 0.42 с (первый батч 0.34 с) | 2.8 млн |
| 20 фрагментов, 5 колонок, batch 1024 | 601 | 910 МБ | 1.36 с | 11.5 млн |
| 20 фрагментов, 5 колонок, batch 8192 | 601 | 910 МБ | 0.67 с | 23.4 млн |
| 20 фрагментов, 5 колонок + `with_row_id` | 601 | 910 МБ | 0.22–0.5 с на 2 фрагмента | — |
| 4 фрагмента, все 104 колонки, batch 1024 | 46 816 | 315 ГБ | 169 с | 18.6 тыс |

- Скан эффективен по сети: чтения по ~16 МБ (`max_iop_size`), 1 GET на файл при открытии, один общий `ScanScheduler` на весь скан.
- Малый `batch_size` удваивает время: 15 353 батча против 1 927 при одинаковом трафике.
- Задержка первого батча 0.3–0.4 с складывается из последовательного открытия файлов фрагмента и первого чтения страниц.
- Полный набор колонок читает ~100 КБ на строку (`sam2_normal`, `cotracker_*`, `visual_qwen3_vl_8b_embedding` на 4096 fp16). Это объём данных, а не узкое место кода.
- `with_row_id` декодирует последовательность фрагмента через `load_row_id_sequence` (кэшируется отдельно от `RowIdIndex`).

## 6. Что можно сделать без правок Lance

1. Держать один `Session` на процесс и передавать его во все `lance.dataset()`: повторное открытие 0.07 с вместо 2.5–3.5 с.
2. Поднять `metadata_cache_size_bytes` до 4–8 ГиБ, чтобы манифест, `RowIdIndex` и метаданные файлов сосуществовали.
3. Для сканов использовать `batch_size` ≥ 8192.
4. При take по row_id группировать запросы: стоимость определяется числом сетевых кругов, а не числом строк. 1000 строк из 10 фрагментов стоят столько же, сколько 100 строк из 100 фрагментов при тёплом кэше.
5. Переписать данные в файлы формата 2.1 при следующей компакции: mini-block кодирование убирает второй зависимый GET для строк при точечном чтении.
6. Прибрать таблицу: `cleanup_old_versions`, осиротевшие `_indices/*` и transaction-файлы. На чтение не влияет напрямую, но убирает сотни ГБ и упрощает листинг.

## 7. Предложения по изменениям в Lance

1. **Нулевое копирование inline row_id при открытии.** Декодировать prost из цепочки чанков (`bytes::Buf`) вместо сборки в `BytesMut`, объявить `inline_row_ids` как `Bytes` в `build.rs` `lance-table`, хранить `Bitmap` как срез тех же байтов. Убирает две копии по ~0.9 ГБ, ~0.5 с CPU и ~1 ГБ пика.
2. **Run-length сегмент row_id** (`RangeWithRuns` или roaring с run-контейнерами) как альтернатива `range_with_bitmap` при выборе представления в `U64Segment`. Для этой таблицы манифест сжался бы примерно вчетверо (890 МБ → ~212 МБ).
3. **Внешний файл row_id на версию.** Формат уже описывает `external_row_ids` (`ExternalFile { path, offset, size }`), но писатели пишут только inline (`rust/lance/src/dataset.rs:3203`, `rust/lance/src/dataset/optimize.rs:2719`), а читатель делает отдельный GET на фрагмент. Один sidecar-файл на версию плюс чтение его чанками сделали бы манифест ~18 МБ, открытие — доли секунды, а загрузку row_id — ленивой.
4. **Одна декодированная копия последовательности.** `load_row_id_index` должен ходить через кэшированный `load_row_id_sequence`, чтобы take и скан с `with_row_id` делили одни `Arc<RowIdSequence>`.
5. **Учёт размера в кэше.** `RowIdIndex` и манифест по отдельности почти равны ёмкости кэша по умолчанию; на таблицах такого масштаба дефолт 1 ГиБ ведёт себя как отсутствие кэша. Варианты: поднять дефолт, вынести `RowIdIndex` в отдельный пул, сделать индекс лениво загружаемым по фрагментам.
6. **Параллельное открытие файлов фрагмента** в `open_readers_impl`: для фрагментов с 5–12 файлами это несколько последовательных сетевых кругов до первого батча и до первого чтения в take.
7. **Защита probe-режима.** Проверять глубину перекрытия диапазонов row_id при компакции или предупреждать, когда она приближается к `MAX_PROBE_DEPTH`, иначе на следующем шаге построение индекса деградирует до O(всех строк).

## 8. Page faults, TLB misses и горячие циклы

Аппаратные счётчики в контейнере доступны (`perf stat -e cycles:u,instructions:u,page-faults,dTLB-load-misses:u,iTLB-load-misses:u`; `dTLB-store-misses` не поддерживается). THP на хосте в режиме `madvise`, поэтому обычный glibc malloc huge pages не получает. Включить их для процесса можно переменной окружения `GLIBC_TUNABLES=glibc.malloc.hugetlb=1` (glibc 2.35: malloc делает `madvise(MADV_HUGEPAGE)` на mmap-чанках). Режим `hugetlb=2` (явные hugetlbfs-страницы) выигрыша не дал.

### 8.1 Декодирование манифеста без сети (`manifest-local`, 2 повтора)

| Метрика | glibc по умолчанию | `hugetlb=1` |
|---|---|---|
| время декодирования | 1.37 с / 0.83 с | 0.37 с / 0.31 с |
| page-faults | 712 062 | 7 317 |
| dTLB-load-misses | 3 039 340 (79% обращений) | 470 017 |
| iTLB-load-misses | 536 431 | 13 124 |
| cycles:u | 2.53 млрд | 1.57 млрд |
| IPC | 0.93 | 1.50 |

712 тыс. minor page faults — это три полные копии ~0.9 ГБ байтов row_id, каждая страница по 4 КБ трогается впервые. Обработка фолтов идёт в ядре и в user-space профиле не видна: поэтому `perf record` показывал 64% в `memmove`, хотя реальная стоимость — не копирование, а фолты на страницах назначения. С huge pages декодирование ускоряется в 2.7 раза при том же числе инструкций.

### 8.2 Полные сценарии с S3

| Сценарий | по умолчанию | `hugetlb=1` |
|---|---|---|
| `DatasetBuilder::load()` (Rust) | 2.4–2.7 с | 1.1–1.4 с |
| `lance.dataset()` (pylance из этого коммита) | 2.58 с, 738 тыс. фолтов | 1.12 с, 58 тыс. фолтов |
| построение `RowIdIndex`, холодное / повторное | 745 / 353 мс | 314 / 291 мс |
| take 1000/10, первый (с построением индекса) | 2.24 с | 1.20 с |
| take 1000/10, тёплый | 0.61–0.71 с | 0.65–0.74 с |
| скан 20 фрагментов, batch 8192 | 1.30 / 1.38 с, 935 тыс. фолтов | 0.78 / 0.85 с, 267 тыс. фолтов |

Тёплый take не меняется: он ограничен сетевыми кругами, а не памятью. Открытие, построение индекса и скан ускоряются в 1.6–2.4 раза только за счёт huge pages. В скане фолты создают буферы GET по 16 МБ (4096 страниц каждый) и Arrow-буферы батчей.

pylance использует системный glibc malloc (собственного `global_allocator` нет), поэтому переменную достаточно выставить в окружении процесса до старта интерпретатора.

### 8.3 Горячие циклы в Rust на этих путях

Профиль `perf record` построения индекса без учёта libc:

| Символ | Доля |
|---|---|
| `FragmentEntry::new` (с инлайном `build_lookup`, `build_bitmap_rank`, `count_ones`) | 4.4% |
| `EncodedU64Array::iter` (замыкание `cloned().map(base + o)` через `Box<dyn Iterator>`) | 3.9% |
| `U64Segment::len` | 1.2% |
| tokio/futures/hyper | ~10% |
| malloc/free | ~8% |

Найденная алгоритмическая проблема: `U64Segment::len` для `RangeWithHoles` считал дыры через `holes.iter().count()`, то есть проходил все элементы `EncodedU64Array` через boxed-итератор с виртуальным вызовом на элемент, хотя `EncodedU64Array::len()` работает за O(1). В этой таблице 3 488 таких сегментов на ~9 млн дыр, а `len()` вызывается на каждый сегмент при построении индекса (`build_lookup`, `start_offset`, `merge_rows`). Патч в `rust/lance-table/src/rowids/segment.rs` (не закоммичен):

```rust
Self::RangeWithHoles { range, holes } => {
    (range.end - range.start) as usize - holes.len()
}
```

Построение `RowIdIndex` после патча:

| Режим | до патча (холодное / повторное) | после патча |
|---|---|---|
| glibc по умолчанию | 745 / 353 мс | 665 / 300 мс |
| `hugetlb=1` | 314 / 291 мс | 265 / 234 мс |

Остальные циклы на горячих путях уже итераторные и упираются в память, а не в форму кода: `count_ones` идёт через `chunks_exact(8)` и `count_ones()` на `u64`, `build_bitmap_rank` через `chunks(256)`, `compute_fragment_offsets` через `scan`, `check_manifest_storage_contract` дедуплицирует списки полей через интернер. Замена `for` на итераторы там ничего не даст. Что ещё можно улучшить на уровне итераторов:

- `EncodedU64Array::iter` и `U64Segment::iter` возвращают `Box<dyn DoubleEndedIterator>`: виртуальный вызов на каждый элемент и невозможность векторизации. Enum-итератор (три конкретных варианта) или generic-замыкание убрали бы это. На текущих путях (open, take, scan без `with_row_id`) они почти не вызываются, поэтому выигрыш будет заметен только для `RowAddrTreeMap::from(&RowIdSequence)`, `decompose_sequence` в merged-режиме индекса и материализации `_rowid` по битмапным фрагментам.
- `Bitmap::iter` разворачивает биты через `flat_map` по 8 бит на байт с `take(len)`; для материализации `_rowid` по `RangeWithBitmap` (1.2 млн бит на фрагмент) быстрее идти по словам `u64` с `trailing_zeros`.
- `FragmentEntry::new` собирает `Vec<SegmentEntry>` через `push` без `with_capacity(sequence.0.len())`, это мелочь на фоне остального.

### 8.4 Побочные наблюдения

- `import lance` занимает 3.2–3.5 с, из них 1.24 с — `lance.namespace`, который эагерно тянет сгенерированный `lance_namespace_urllib3_client` со всеми API-модулями (`python/python/lance/__init__.py:66`). Ленивый импорт убрал бы треть времени старта.
- Тирдаун процесса после `del ds` и завершения интерпретатора укладывается в доли секунды: 63 потока tokio выходят в одну и ту же секунду, утечки ожидания нет.

## 9. Артефакты и окружение

- Харнесс: `rust/lance/examples/shot_table_profile.rs` (не закоммичен). Сборка: `cargo build --profile bench --example shot_table_profile -p lance`. Режимы `open <uri>`, `manifest-local <file> [repeats]`, `rowid-index <uri> [repeats]`, `take <uri> <n_rows> <n_frags> <repeats> [cols]`, `scan <uri> <n_frags> <batch_size> [cols] [with_row_id]`. Переменные `LANCE_CHROME_TRACE=<file>` (chrome-trace спанов) и `LANCE_META_CACHE_GB=<n>`.
- Скрипты разбора манифеста и трейсов в scratchpad сессии: `manifest_stats.py`, `rowid_segments.py`, `overlap_depth.py`, `bitmap_runs.py`, `trace_stats.py`, `py_bench.py`.
- `perf`: `/usr/lib/linux-tools-5.15.0-191/perf`, только user-space (`perf_event_paranoid=2`, нет CAP_SYS_ADMIN). В корневом `Cargo.toml` нет профилей `release-with-debug`/`release-no-lto`, упомянутых в CLAUDE.md, использован `bench`.
- Для сборки нужен `protoc` (установлен 29.3 в `/usr/local/bin`), без него падают build-скрипты `lance-encoding`/`lance-table`.
- Патч `U64Segment::len` в `rust/lance-table/src/rowids/segment.rs` оставлен в рабочем дереве незакоммиченным.
- Для сравнения: установленный в другом окружении pylance 7.0.0-beta.12 на этой же таблице открывается 12.2 с и делает первый take за 114 с. Все цифры выше относятся только к `main`.
